请你作为资深前端开发工程师，根据以下需求和设计规范，使用 **React (Next.js 15+ App Router)** 和 **Tailwind CSS** 框架，生成一个高质量、现代化、响应式的单页面应用（SPA）原型代码。代码应结构清晰、组件化，并包含适当的交互状态。

**项目名称**：宁翼智能科技官网 - 企业AI管家

**一、核心要求**
1.  **技术栈**：Next.js 15 (App Router), React, TypeScript, Tailwind CSS。
2.  **风格**：专业、科技感、工业风。遵循提供的配色方案。
3.  **响应式**：完美适配桌面、平板、手机。
4.  **交互**：
    *   固定导航栏，滚动高亮当前章节。
    *   “Demo展示区”的对话气泡在页面加载后应有淡入效果。
    *   “客户价值区”的数字应实现滚动到视窗时从0开始的计数动画。
    *   “解决方案区”使用可点击的标签页进行内容切换。
5.  **组件化**：将页面拆分为可复用的组件（如 `Header`, `HeroSection`, `ValueCard`, `DemoShowcase`, `CaseStudyCard` 等）。

**二、设计规范**
*   **配色**：
    *   主色：`#0A1F3E` (深蓝)
    *   强调色1：`#3A86FF` (科技蓝)
    *   强调色2：`#8338EC` (未来紫)
    *   背景：`#FFFFFF`, `#F8F9FA`
    *   文字：`#1F2937` (灰黑), `#6B7280` (灰)
*   **字体**：使用 `font-sans`，在 `tailwind.config.js` 中优先配置 `Inter` 字体族。
*   **圆角**：中等圆角，例如 `rounded-xl`。
*   **阴影**：卡片使用柔和阴影，例如 `shadow-lg`。

**三、页面结构与内容 (按顺序实现以下区块)**
请根据第一部分“网站逻辑与内容结构文档”中**第二点（各区块详细逻辑与内容扩写）** 的描述，实现从 **1. 顶部导航栏** 到 **12. 最终行动号召区** 的所有内容。请严格使用该部分提供的**标题、文案、数据、示例文本**。

**重点区块实现提示**：
1.  **导航栏**：固定定位，背景半透明毛玻璃效果。Logo放左侧，导航链接居中，两个按钮在右侧。
2.  **Hero区**：全屏高度，深蓝色渐变背景，标题文字大且醒目，按钮醒目。
3.  **核心价值区**：四个等宽的 `ValueCard` 水平排列（移动端垂直堆叠），每个卡片包含图标、粗体数据、简短描述。
4.  **Demo展示区**：创建一个视觉上类似聊天界面的区域，左右对话气泡，使用提供的示例文案。右侧AI回复气泡背景为浅蓝色 (`bg-blue-50`)。
5.  **产品能力区**：三个等宽卡片水平排列，每个有图标、小标题和描述。
6.  **客户价值区**：三个大数字水平排列，使用 `react-countup` 或类似逻辑实现滚动触发计数动画。
7.  **产品架构图区**：创建一个垂直的、分层的可视化图表。可以用多个水平居中的 `div` 层叠，用竖线连接，每层有标签描述。使用主色和强调色区分层级。
8.  **解决方案区**：实现一个标签页组件。默认选中第一个标签，点击标签切换下方显示的内容（内容为对应解决方案的描述列表）。
9.  **成功案例区**：实现一个水平滚动的案例卡片列表（或移动端堆叠），每张卡片包含标题、痛点、解决方案、结果。
10. **最终行动号召区**：背景使用深色，文字反白，包含一个简单的联系表单（四个输入框：姓名、公司、电话、需求，和一个提交按钮）或两个大按钮。

**四、输出要求**
1.  提供一个完整的、可运行的 `Next.js` 项目核心文件结构。
2.  包括：`app/page.tsx` (主页面), `app/layout.tsx` (根布局，配置元数据和字体), `app/components/` 文件夹下的关键组件文件（至少5个），`tailwind.config.js` 和 `postcss.config.js` 配置文件。
3.  在 `app/page.tsx` 中导入并使用你创建的组件来组装页面。
4.  代码注释清晰，TypeScript 类型定义完善。
5.  不需要后端API代码，表单提交可暂时用 `console.log` 处理。

